# Google AI API Configuration
# Get your API key from: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your_google_api_key_here

# Optional: Customize Gemini model (default is gemini-pro)
# Available models: gemini-pro, gemini-1.5-pro, gemini-1.5-flash, gemini-2.0-flash-exp, gemini-2.0-pro-exp
# GEMINI_MODEL=gemini-2.0-flash
# 
# Note: Run 'python3 gemini_text_to_knowledge.py --api-models' to see which models are accessible with your API key

# Optional: Set custom output directory (default is 'output')
OUTPUT_DIR=output

# Optional: Set custom input directory (default is 'input')
INPUT_DIR=input

# Optional: Show detailed model list during processing (default is false)
SHOW_MODEL_LIST=false

# Optional: Rate limiting configuration
# RATE_LIMIT_PAUSE=60              # Pause duration in seconds (default: 60)
# DISABLE_RATE_LIMIT=false         # Set to 'true' to disable rate limiting

# Optional: Profanity filtering mode
# REMOVE_PROFANITY_COMPLETELY=false # Set to 'true' to completely remove profanity instead of masking with ****
